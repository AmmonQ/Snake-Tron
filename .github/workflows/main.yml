name: Run Browserify
on: [push]
jobs:
  Run-Browserify:
    runs-on: ubuntu-latest
    steps:
      - run: "ls"
      - run: "npm install -g browserify"
      - run: "browserify shared/constants.js public/js/client.js -o public/js/bundle.js
